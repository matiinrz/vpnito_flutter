class Server {
  String? flag;
  String? name;
  String? domain;
  String? username;
  String? password;
  int? port;
  int? mtu;

  Server({this.flag, this.name, this.domain, this.username, this.password, this.port, this.mtu});
}